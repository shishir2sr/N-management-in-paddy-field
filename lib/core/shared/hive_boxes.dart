/// Hive box names, so the string is declared once rather than repeated at every
/// `Hive.box(...)` call site.
class HiveBoxes {
  const HiveBoxes._();

  static const String resultState = 'resultStateBox';
}
